---
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Release.Name }}-vdc
  labels:
    release: {{ .Release.Name }}
  namespace: {{ .Release.Namespace}}
stringData:
  {{ .Release.Name }}: {{ randAlphaNum 16 | lower | b64enc }}
