---
apiVersion: v1
kind: Secret
metadata:
  name: mgmtapi_login-info-secret
  labels:
    release: {{ .Release.Name }}
  namespace: {{ .Release.Namespace}}
data:
  username: {{ .Values.deos-hc.username | b64enc | quote}}
  password: {{ .Values.deos-hc.password | b64enc | quote}}