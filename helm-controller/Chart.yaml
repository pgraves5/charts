---
apiVersion: v2
name: helm-controller
version: 0.33.0
appVersion: 1.16.0
description: |
  helm-controller runs inside the cluster and acts as a helm broker to perform
  helm actions for packages, i.e install, update, delete
maintainers:
  - name: Dell EMC
    url: https://dellemc.com
dependencies:
  - name: objectscale-graphql
    version: 0.33.0
    repository: file://../objectscale-graphql
    condition: graphql.enabled
