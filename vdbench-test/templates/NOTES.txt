Thank you for installing {{ .Chart.Name }}. This release is capable of
deploying and upgrading vdbench-test chart.


Your release is named {{ .Release.Name }}.

To install k8s-vdbench-test by using Helm chart, use the helm install command below:

  $ helm install <repository name>/vdbench-test

For more information on the installation and configuration options, see the
README at https://github.com/EMCECS/charts/tree/master/vdbench-test and the
`values.yaml` file at
https://github.com/EMCECS/charts/blob/master/vdbench-test/values.yaml
