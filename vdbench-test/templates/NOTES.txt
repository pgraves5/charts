Thank you for installing {{ .Chart.Name }}. This release is capable of
deploying and upgrading Kubernetes Application Health Managements.


Your release is named {{ .Release.Name }}.

To install k8s-health-checks by using Helm chart, use the helm install command below:

  $ helm install <repository name>/k8s-health-checks

For more information on the installation and configuration options, see the
README at https://github.com/EMCECS/charts/tree/master/k8s-health-checks and the
`values.yaml` file at
https://github.com/EMCECS/charts/blob/master/k8s-health-checks/values.yaml
