Thank you for installing {{ .Chart.Name }}. This release is capable of
deploying a Dell EMC k8s Dell EMC k8s customercontact resource.


Your release is named {{ .Release.Name }}.

To install dellemc-customercontact by using Helm chart, use the helm install command below:

  $ helm install <repository name>/dellemc-customercontact --set-file customercontactfile=<location of customer contact yaml file> --set product=<product name>

For more information on the installation and configuration options, see the
README at https://github.com/EMCECS/charts/tree/master/dellemc-customercontact and the
`values.yaml` file at
https://github.com/EMCECS/charts/blob/master/dellemc-customercontact/values.yaml
