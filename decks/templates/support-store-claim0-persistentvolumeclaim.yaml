apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: decks-support-store-claim
  name: decks-support-store-claim0
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: {{ .Values.supportStore.persistentVolume.size }}
status: {}
