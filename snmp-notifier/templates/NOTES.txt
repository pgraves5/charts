Thank you for installing {{ .Chart.Name }}. This release is capable of
deploying and upgrading Dell Technologies KAHM SNMP Notifier

Your release is named {{ .Release.Name }}.

Use the helm install command below to deploy snmp-notifier 

  $ helm install snmp-notifier <repository name>/snmp-notifier

For more information on the deployment options, Refer to:
README.md at          https://github.com/EMCECS/charts/tree/master/snmp-notifier 
`values.yaml` file at https://github.com/EMCECS/charts/blob/master/snmp-notifier/values.yaml

