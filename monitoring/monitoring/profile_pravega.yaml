#
# Copyright © [2020] Dell Inc. or its subsidiaries.
# All Rights Reserved.
#
# This software contains the intellectual property of Dell Inc.
# or is licensed to Dell Inc. from third parties. Use of this
# software and the intellectual property contained therein is expressly
# limited to the terms and conditions of the License Agreement under which
# it is provided by or on behalf of Dell Inc. or its subsidiaries.
#
#

## Default values.yaml for Pravega System Monitoring
## This is a YAML-formatted file.
---
global:
  tls_enabled: false
  telegraf_tls_enabled: false
  # communication_scheme defined separately from tls_enabled as found no way to make conditional set in values.yaml
  communication_scheme: http
  influxdb_replicas: 1
  rsyslog_enabled: true
  prometheus_alerts_enabled: false

fluxd:
  replicaCount: 1
  resources:
    requests:
      memory: 300Mi
    limits:
      memory: 300Mi
grafana:
  resources:
    requests:
      memory: 300Mi
    limits:
      memory: 300Mi
influxdb:
  persistence:
    storageClassName: "standard"
  resources:
    requests:
      memory: 2Gi
    limits:
      memory: 2Gi
  disable_recovery: true
telegraf:
  resources:
    requests:
      memory: 256Mi
    limits:
      memory: 256Mi
telegraf-ds:
  resources:
    requests:
      memory: 256Mi
    limits:
      memory: 256Mi
throttler:
  resources:
    requests:
      memory: 300Mi
    limits:
      memory: 300Mi
rsyslog:
  resources:
    requests:
      memory: 256Mi
    limits:
      memory: 256Mi
