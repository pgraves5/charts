apiVersion: v1
data:
    dbType: {{.Values.db.dbType }}
    eventTtl: "{{.Values.db.eventTTL }}"
kind: ConfigMap
metadata:
  name: kahm-db-config
  namespace: default
  labels:
    release: {{ .Release.Name }}
